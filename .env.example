# These env vars are passed via your MCP client config (not loaded from this file).
# See README or run `npm run setup` for automatic configuration.
#
# Ghost Admin → Settings → Integrations → Add custom integration → Copy API key
GHOST_URL=https://your-ghost-blog.com
GHOST_ADMIN_API_KEY=your_id:your_hex_secret
